#### char类型数组和null字符
C语言没有专门存储字符串变量类型，字符串都被存储在char数组中。数组由连续的存储单元组成。
数组末尾位置 \0 是空字符，40存储单元字符串实际上只能存储39字符。
```c
scanf("%s",name)
> abc def
输出 abc
```
scanf（）一般只会读取第一个单词，它遇到空格，制表符换行符就不再读取输入

#### strlen（）函数
sizeof字节单位给出对象大小。strlen()函数给出字符串的字符长度。

#### 明示常量
使用C预处理器定义常量，
```c
# define TAXRATE 3.14
```
在编译程序时候所有TAXRATE都会替换成3.14 这一过程被称之为**编译时替换**
![[89622cab193ad3c9e9e9856dc012951c.jpg | 500]]

#### const限定符
C90新增了const关键字，用于先顶一个变量为只读

#### 转换说明
![[438ecd76e3558305b44cce16ebd3845e.jpg | 500]]

、