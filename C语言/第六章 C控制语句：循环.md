#### while循环
```c
short index = 1
while( --index < 5 )
```
当index减少到正数  32767 short有符号存储的最大值时，会停止循环

#### 真值问题
scanf（“%d”,&num）函数，如果读取的不是num对应数据类型，会返回0
如果在循环中（非循环也一样），scanf（）读取非对应数据类型，会将读取错误的值放在队列下次读取，用户无法介入，会导致死循环。